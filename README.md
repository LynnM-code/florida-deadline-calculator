# florida-deadline-calculator

## Streamlit deployment

If you deploy this repository to Streamlit Cloud, set the "Main file" (entrypoint) to:

```
deadline-calculator-app-v8.py
```

Run locally with:

```bash
pip install -r requirements.txt   # ensure streamlit is listed
streamlit run deadline-calculator-app-v8.py
```
